version: '3.8'

services:
  student-management:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
